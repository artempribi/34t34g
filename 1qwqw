echo "# 34t34g" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/artempribi/34t34g.git
git push -u origin main
echo "# 34t34g" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/artempribi/34t34g.git
git push -u origin main
echo "# 34t34g" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/artempribi/34t34g.git
git push -u origin main
